import 'package:flutter/material.dart';

class MovesDetailsPage extends StatelessWidget {
  @override
  Widget build(BuildContext context) {
    return Center(
        child: Column(
      children: [Text("moves")],
    ));
  }
}
